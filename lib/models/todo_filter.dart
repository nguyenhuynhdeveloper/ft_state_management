// Enum để định nghĩa các loại filter cho todo list
enum TodoFilter {
  all,       // Hiển thị tất cả todos
  active,    // Chỉ hiển thị todos chưa hoàn thành
  completed, // Chỉ hiển thị todos đã hoàn thành
}
